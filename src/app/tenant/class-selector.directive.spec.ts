import { ClassSelectorDirective } from './class-selector.directive';

describe('ClassSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
